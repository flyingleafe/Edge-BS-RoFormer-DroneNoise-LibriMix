# The paper regime matrix

*(Stub — the orchestrator owns this document. It is created here so the
per-experiment docs of the matrix have a batch document to point at.)*

## Motivation

One table for the paper: every rotor-speed-prediction row at a stated training
regime, a stated trunk and a stated microphone count, so a difference between
two rows has exactly one cause. The configurations that fill it are added by
the `ladder-configs` branch in two waves — the regime-matched reruns and the
speech A/B first, then the reality ladder.

## Results

Pending.

## Conclusion

Pending.
