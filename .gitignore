*.pyc
# data/ and datasets/ contents are tracked by DVC; only the .dvc pointer files
# and the DVC-managed nested .gitignore files live in git.
/data/*
!/data/.gitignore
!/data/*.dvc
/datasets/*
!/datasets/.gitignore
!/datasets/*.dvc
wandb/
results/
.direnv/
.env
*.xml

drones-with-speeds.md
.ipynb_checkpoints/
.claude/worktrees/

.venv/
node_modules/

# Trello project config (may contain workspace names)
.trello

# postdoc runtime state (runs, worktrees, FIFO, logs)
.postdoc/
