# Train + Eval: DCCRN+RPS on DREGON-LM
model:
  type: dccrn
  base_config: configs/10b_DCCRN_RPS_DREGON.yaml

dataset:
  name: DREGON-LM

eval:
  metrics: [estoi, si_sdr, pesq]

wandb:
  tags: [dccrn, rps, dregon]
