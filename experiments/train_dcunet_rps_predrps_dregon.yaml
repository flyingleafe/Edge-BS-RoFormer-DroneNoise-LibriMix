# Train + Eval: DCUNet+RPS with auxiliary RPS prediction on DREGON-LM
model:
  type: dcunet
  base_config: configs/7c_DCUNet_RPS_PredRPS_DREGON.yaml

dataset:
  name: DREGON-LM

eval:
  metrics: [estoi, si_sdr, pesq]

wandb:
  tags: [dcunet, rps, predrps, dregon]
