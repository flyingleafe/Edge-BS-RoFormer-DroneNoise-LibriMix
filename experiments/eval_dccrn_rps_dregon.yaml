# Eval-only: DCCRN+RPS on DREGON-LM valid split
eval_only: true
checkpoint: results/85eddadc0e33/training/best_model.ckpt

model:
  type: dccrn
  base_config: configs/10b_DCCRN_RPS_DREGON.yaml

dataset:
  name: DREGON-LM

eval:
  metrics: [estoi, si_sdr, pesq]

wandb:
  tags: [eval, dccrn, rps, dregon]
