# Train: DCCRN encoder + RPSPredictionHead, RPS-only on DREGON-LM
model:
  type: dccrn
  base_config: configs/11b_DCCRN_RPSOnly_DREGON.yaml

dataset:
  name: DREGON-LM

training:
  metrics: [neg_mse]
  metric_for_scheduler: neg_mse

wandb:
  tags: [dccrn, rps-only, dregon]
