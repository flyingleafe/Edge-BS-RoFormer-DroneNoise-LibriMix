# Example experiment — reference for creating new experiments
model:
  type: dcunet
  base_config: configs/5_Baseline_dcunet.yaml
  overrides:
    training.lr: 3.0e-4
    training.batch_size: 4

dataset:
  name: DN-LM

run:
  max_duration: 6h
  checkpoint_interval: 30m

wandb:
  tags: [example, dcunet]
