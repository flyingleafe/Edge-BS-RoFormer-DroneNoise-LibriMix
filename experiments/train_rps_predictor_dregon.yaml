model:
  type: rps_predictor
  base_config: configs/11c_RPSPredictor_DREGON.yaml

dataset:
  name: DREGON-LM

training:
  metrics: [neg_mse]
  metric_for_scheduler: neg_mse

wandb:
  tags: [rps-predictor, rps-only, dregon]
