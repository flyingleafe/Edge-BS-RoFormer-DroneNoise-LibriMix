# @package _global_
# DCCRN baseline, no RPS, on DREGON-LM.
defaults:
  - override /data: dregon_lm_v1
  - override /model: b1_dccrn_baseline_dregon
  - override /loss: masked_mse
  - override /metrics: separation_basic

experiment_name: b1_dccrn_baseline_dregon
optim:
  monitor: si_sdr
  monitor_mode: max
