---
experiment: gen_r2_orig_perrotor
training_config: conf/experiment/gen_r2_orig_perrotor.yaml
batch: docs/experiments/generator-label-sensitivity.md
---

# `gen_r2_orig_perrotor`

## Motivation

The per-rotor cell of the label A/B, on ORIGINAL telemetry.

Per-rotor sub-embeddings let the four rotors differ in timbre instead of
sharing one code. Against `gen_r1_orig` this arm gives the value of per-rotor
identity at unchanged labels — the control the refined per-rotor arm needs.

Full batch context: [Generator label sensitivity](../../docs/experiments/generator-label-sensitivity.md).

## Conclusion

Pending — the arm is not run yet.
