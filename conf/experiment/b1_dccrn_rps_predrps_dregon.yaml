# @package _global_
# DCCRN + encoder RPS conditioning + auxiliary RPS-prediction head (rps_aux_weight=2.0) on DREGON-LM.
defaults:
  - override /data: dregon_lm_v1
  - override /model: b1_dccrn_rps_predrps_dregon
  - override /loss: masked_mse_plus_pit_rps_w2
  - override /metrics: separation_plus_rps

experiment_name: b1_dccrn_rps_predrps_dregon
optim:
  monitor: si_sdr
  monitor_mode: max
