# @package _global_
# DPTNet baseline (Paper 1 comparison point), on DN-LM. See REPLICATION.md § A1.
defaults:
  - override /data: dn_lm
  - override /model: a1_baseline_dptnet
  - override /loss: masked_mse
  - override /metrics: separation_basic

experiment_name: a1_baseline_dptnet
optim:
  monitor: si_sdr
  monitor_mode: max
