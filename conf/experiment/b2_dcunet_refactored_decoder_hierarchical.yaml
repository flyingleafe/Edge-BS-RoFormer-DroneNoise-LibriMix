# @package _global_
# DCUNetRefactored, decoder-side hierarchical RPS fusion, on DREGON-LM.
defaults:
  - override /data: dregon_lm_v1
  - override /model: b2_dcunet_refactored_decoder_hierarchical
  - override /loss: masked_mse
  - override /metrics: separation_basic

experiment_name: b2_dcunet_refactored_decoder_hierarchical
optim:
  monitor: si_sdr
  monitor_mode: max
