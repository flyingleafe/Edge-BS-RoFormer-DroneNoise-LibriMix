# @package _global_
# RPS-DCUN6-P (Gulli et al. EURASIP 2025) — DCUNet + encoder bottleneck RPS fusion on DREGON-LM.
defaults:
  - override /data: dregon_lm_v1
  - override /model: b1_dcunet_rps_dregon_bottleneck
  - override /loss: masked_mse
  - override /metrics: separation_basic

experiment_name: b1_dcunet_rps_dregon_bottleneck
optim:
  monitor: si_sdr
  monitor_mode: max
