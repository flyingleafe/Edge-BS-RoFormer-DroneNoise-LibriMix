# @package _global_
# RPS-DCUN5 — DCUNet + encoder GRU RPS fusion on DREGON-LM.
defaults:
  - override /data: dregon_lm_v1
  - override /model: b1_dcunet_rps_dregon_gru
  - override /loss: masked_mse
  - override /metrics: separation_basic

experiment_name: b1_dcunet_rps_dregon_gru
optim:
  monitor: si_sdr
  monitor_mode: max
