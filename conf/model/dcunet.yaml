# DCUNetRefactored — speech_enhancement, via the legacy
# utils.get_model_from_config dispatch (model_type + a pre-existing legacy
# YAML that carries the architecture hyperparameters DCUNet needs, e.g.
# audio.n_fft/audio.chunk_size). No RPS conditioning (baseline).
task: speech_enhancement
task_params:
  n_channels: null
  use_rps: false
  predict_rps: false
  sr: [16000, 1]
_target_: null
params: {}
model_type: dcunet_refactored
legacy_config_path: configs/12a_DCUNetRefactored_baseline.yaml
