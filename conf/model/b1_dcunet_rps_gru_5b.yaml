# b1_dcunet_rps_gru_5b — DCUNet + encoder-side RPS, GRU fusion (5-layer). Dataset ambiguous — see REPLICATION.md § B1 caveat.
task: speech_enhancement
task_params:
  n_channels: null
  use_rps: true
  predict_rps: false
  sr: [16000, 1]
  rps_rate: [16000, 512]
_target_: null
params: {}
model_type: dcunet
legacy_config_path: configs/5b_DCUNet_RPS_gru.yaml
