# b1_dcunet_rps_bottleneck_5a — DCUNet + encoder-side RPS, bottleneck fusion (6-layer). Dataset ambiguous (predates the _DREGON config-naming split; best-effort mapped to conf/data/dregon_lm_v1 — see REPLICATION.md § B1 caveat).
task: speech_enhancement
task_params:
  n_channels: null
  use_rps: true
  predict_rps: false
  sr: [16000, 1]
  rps_rate: [16000, 512]
_target_: null
params: {}
model_type: dcunet
legacy_config_path: configs/5a_DCUNet_RPS_bottleneck.yaml
