# Critique round 1 (REVISE)

1. [layout] (page 8, Table 2 'FLY124 fix-by-fix evolution') Literal '+' prefixes in the fix-column cells (report.typ lines 330 and 332) are parsed by Typst as enum markers, so both rows render as '1. residual re-scan (arm R)' and '1. per-track stage guard' — the cumulative '+' meaning is lost and two different fixes both appear numbered '1.' — fix: Escape the plus as \+ (or reword to 'then: residual re-scan') in both table cells so the intended '+ fix' notation renders literally.
2. [figures] (page 10, Figure 6 (vk_parity_bars.png)) The tallest bar's value label '2.87' is hidden behind the legend box — only a clipped fragment is visible, so the key DREGON phase-B number is unreadable in the figure. — fix: Regenerate vk_parity_bars.png with a higher y-limit or the legend moved (upper right / outside axes) so all bar value labels are visible.
3. [figures] (page 7, Figure 3 (vk_alias_illustration.png)) Stray grey annotation text ('scan_f_max ...') is clipped at the plot's top-right corner and collides with the legend box, looking like a rendering accident. — fix: Reposition the scan_f_max annotation below the dashed cutoff line (or remove it — the dashed line plus caption already convey the cap) and regenerate the asset.
4. [clarity] (page 10, 'Verdict and roadmap') The quantified parity gap is stated with phase-B numbers (2.87 DREGON, 1.90 FLY124), but the report itself shows phase A is better (2.62 / 1.55); quoting the worse lever overstates the gap and is inconsistent with the abstract's 'best neural predictor'. — fix: State the gap against the best neural result: 'best neural 2.62 vs. 0.68–0.74 rev/s on DREGON, 1.55 vs. 1.03 on FLY124', keeping the note that phase B was worse.
5. [tone] (pages 9–10, 'In build' paragraphs in Fast inference and Neural parity sections) Bracketed markers '[TODO verify]' and '[TODO verify — zero numbers yet]' read as leftover draft scaffolding rather than deliberate pending flags. — fix: Keep the pending status but phrase it plainly, e.g. '*In build, no numbers yet:*' — this still satisfies the narrative's 'explicitly marked pending' rule.
