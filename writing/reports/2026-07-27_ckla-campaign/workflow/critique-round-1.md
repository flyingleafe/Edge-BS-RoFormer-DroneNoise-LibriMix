# Critique round 1 (REVISE)

1. [clarity] (throughout (pages 3, 4, 6, 8, 9, 10) — every "§N" cross-reference) The text references sections by number ("§3", "§4", "§4.1", "§4.3", "§5", "§6", ~12 occurrences) but the template renders all headings unnumbered (see TOC page 2), so none of these references resolve for the reader. — fix: In report.typ add `#set heading(numbering: "1.1")` after the show rule (or convert the §N mentions to Typst @-references to labeled headings so they render as live section numbers/names).
2. [clarity] (page 3, "The augmentation regime: dilution, not futility", last paragraph) Leftover pre-correction arithmetic: "present at roughly an order of magnitude below the dose" and "one useful ingredient at 8% strength" contradict the corrected numbers in Figure 1 (1-in-9 ≈ 11.7% per chunk, ~6x dose increase) that the creator log says were fixed elsewhere. — fix: In report.typ change "roughly an order of magnitude below" to "roughly 6x below" (or "at a sixth of the solo dose") and "8% strength" to "~11% strength" to match Figure 1.
3. [layout] (running header, all pages 2-11) The full report title in the running header wraps to two ragged lines (centered first line, flush-left second line with the page number), looking broken on every body page. — fix: Pass a short running-header title in report.typ (e.g. "HNS — CKLA campaign") if the template supports it; otherwise shorten the header string locally so it fits on one line.
