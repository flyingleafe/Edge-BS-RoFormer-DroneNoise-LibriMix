Critic round 2 said REVISE:
1. [layout] (whole deck (check/page-01..23.png, check/_sheet2.png)) Rendered pages are stale: slides.typ contains the round-2 edits (gloss lines 53-57, pooled FLY124 line 327-330, new ramp-handling schematic 146-152, tau_k note 515) but check/page-03, -05, -10, -13, -14, -17 still show the round-1 content (old dia_vk_loop diagram, no gloss, no pooled line, unchanged charts). The critic cannot review unrendered work. — fix: Run make check inside writing/slides/2026-08-04_rps-tracking-results-and-paper-plan to recompile the PDF and regenerate all check/page-*.png plus the contact sheet, then resubmit.
2. [figures] (pages 13-14 (lock ladder + beamform charts)) Cannot make sure from source that the round-1 figure fixes landed: the stale renders still show the unlabeled 0.5 dashed line on the lock ladder versus the ~0.7 label on the beamform chart, the small beamform chart with the threshold label outside the axes, and the chart title "collapses when the second motor starts" versus a single-motor -> 4-motors x-axis. — fix: If prepare_figs.py was not updated, apply the round-1 figure fixes (same labeled ~0.7 threshold on both charts, larger beamform panel with the label inside the axes, retitled lock-ladder chart), regenerate the PNG assets, and include them in the fresh render.
