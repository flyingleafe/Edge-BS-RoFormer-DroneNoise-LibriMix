Critic round 1 said REVISE:
1. [figures] (page 4 / slide "The VK chain, step 2 — ramp handling") The right-hand diagram (assets/dia_vk_loop.png) shows the fix-track/solve-envelopes iteration loop plus a red caveat about needing a pitch guess and being too slow to run inside a network. That content belongs to step 4 (coupled envelope solve), not ramp handling, and the caveat is never referenced anywhere. A cold reader cannot connect it to the octave/blade-pass story on the left. — fix: Replace the image with a warm-up spectrogram panel that shows the even/blade-pass-only lines (data in results/beatvk_vk_arms/, generated via the deck's prepare_figs.py), or drop the image and let the 15–36 → 2.9–4.0 MAE number carry the slide.
2. [narrative] (page 10 / slide "Output comparison — FLY124 cruise window") The chosen window shows blind VK (0.95) beating CKLA (1.06) — the opposite of the narrative's intended message for this slide (neural is the better FLY124 tracker; neither family dominates). That spine lives only in speaker notes, so the visual story a cold reader takes away is "VK wins both drones". — fix: Select a FLY124 cruise window where CKLA beats blind VK, or add one takeaway line on the slide with the pooled numbers ("pooled over 4 windows: CKLA 1.08* vs blind VK 2.38 — this window is VK's best").
3. [clarity] (page 3 / slide "Headline results — fixed 15-window raw protocol" (first use, deck-wide)) CKLA, KLA, and pi_kalman are never expanded on any slide, and "VK" is expanded (Vold–Kalman) only in speaker notes. The narrative requires the deck to stand alone for a supervisor who is lost in the weekly reports. — fix: Add a one-line gloss on the headline slide or agenda, for example: "VK = Vold–Kalman comb tracker; CKLA/KLA = our neural tracker family; pi_kalman = phase-increment refiner".
4. [clarity] (page 17 / slide "Significance") Bullet "Required RPS precision for SE: now known" asserts a result the deck never states — no slide gives the actual precision number/budget. The style guide flags unbacked claims as a recurring bounce cause. — fix: State the number on this slide or on the Model v2 slide (the measured budget, e.g. the tau_k coherence-time figures or the rev/s precision the SE conditioning must meet).
5. [figures] (pages 13–14 / slides "The lock measurement" and "Use more microphones? — no") The lock-ladder dashed threshold sits at 0.5 and is unlabeled, while the next slide labels "needed for lock (~0.7)" — two different implied thresholds for the same quantity. The beamform chart is also small (height 58%) with the threshold label hanging in tiny green text outside the axes. — fix: In prepare_figs.py, put the same labeled ~0.7 threshold on the lock ladder, enlarge beamform.png (height ~70%), and move the threshold label inside the plot area.
6. [layout] (page 13 / slide "The lock measurement — the realism ladder") The chart title says "collapses when the second motor starts" but the x-axis jumps from "single motor" directly to "4 motors" — a small mismatch a cold reader trips on. — fix: Retitle the figure "collapses as soon as more than one motor runs" (or add a 2-motor rung if that measurement exists in results/vk_phase_validation_decomp).
