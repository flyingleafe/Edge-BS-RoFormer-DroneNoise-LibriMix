Critic round 2 said REVISE:
1. [figures] (page 12, slide "The decomposition on Michael's rig: FLY124 and FLY125") Both spectrogram figures are clipped. The y-axis labels and frequency tick labels are cut off entirely, the panel captions are chopped at the left edge ("— tracked amplitude and phase per harmonic", "b — rotor-locked energy...", "hat is left: the turbulent floor"), the top panel has no visible caption at all, and the right end of the time axis is truncated ("5" instead of "52"). A cold reader cannot tell which panel is which or what the frequency range is. — fix: In slides.typ lines 435-436 replace `image("assets/spec_fly124.jpg", height: 88%)` (and the FLY125 twin) with `width: 100%` so the figure is fitted to the column instead of overflowing it; widen the two figure columns (e.g. `columns: (1fr, 1fr, 0.9fr)`) and drop the panel-caption prefix duplication so the captions fit.
2. [figures] (pages 2, 6 and 11 (slides 1, 5, 10)) The identical four-panel DREGON figure `assets/spec_dregon.jpg` appears three times. On slide 1 and slide 5 it shows the finished comb/broadband split — the deck's punchline — before the decomposition has been introduced, and its panel captions use terms ("coherent comb", "stochastic comb") that are never defined on any slide. By page 11 the reader has no reason to look at it again. — fix: Keep the full four-panel figure only on page 11 (the results slide) and add one sentence naming the four panels. On page 6 ("The requirement") crop to the single `original (mic 1)` panel — that is the slide's actual point (audio is messy). On page 2 drop the figure and use the space for the recap text and question box, or show the tooth-definition sketch already available as `assets/tooth_def.png`.
3. [layout] (pages 4, 5, 8, 10, 13, 14) Roughly a third to a half of each of these slides is blank. The `#v(1fr)` spacers push a narrow band of content into the vertical middle, leaving large empty margins at top and bottom, and the figures sit at a size where axis tick labels are near the readable limit. narrative.md's constraint 3 says explicitly "Use the full slide area... Avoid large white space." Page 13 and page 14 are more than half empty. — fix: Remove the surrounding `#v(1fr)` pairs on these slides and let content start under the title, then scale each figure up to fill the recovered height (e.g. the linewidth-law and Whittle-cost images to `width: 100%` in a wider column). On pages 13 and 14 either merge the two slides or add the figure the narrative asks for.
4. [tone] (pages 3, 4, 5, 6, 11 (right-hand columns)) Most content slides carry multi-sentence paragraph prose, against narrative.md constraint 2 ("Bullets of at most ~8 words. No paragraph text on slides."). Page 3's right column is three separate paragraphs; page 4's main sentence runs ~30 words ("The window's magnitude fluctuates around a low median, and a log-L1 loss fits the median: a permanent downward gradient on every steady line above k ≈ 25"), over the 20-word STE limit the narrative sets. — fix: Convert each of these paragraphs into 2-4 bullets of <=8 words and move the full sentences into the existing `#speaker-note` blocks, which already carry the same content. Example for page 3: "Refined wins in every band" / "Only arm above the null at k50-80" / "Label fix necessary, not sufficient".
5. [clarity] (page 13, slide "This objective also ranks trajectory hypotheses") The two-row table has no header and reads as broken English: row 1 "refined beats telemetry | 4 of 5 windows", row 2 "the one loss, per cell | by 0.049". A cold reader cannot tell that row 2 describes the losing fifth window, nor what 0.049 is measured in, nor whether that margin is large or small. — fix: Replace the table with one line: "Refined labels score better on 4 of 5 frozen windows. On the fifth they lose by 0.049 per cell — inside the run-to-run spread." Or keep a table with headers `window set | refined vs. telemetry | margin`.
6. [figures] (page 9, slide "Objective, part 2: linewidth and marginalization") The figure title "Marginal PSD: floor plus lines sum to M, one number per cell" collides with the "H = 9.0" annotation of the left peak; the two strings overprint and both become hard to read. — fix: Regenerate `assets/marginalization.png` with more headroom above the peaks (raise the y-limit ~15 %) or move the `H = 9.0` label to the right of its arrow, below the title band.
7. [clarity] (pages 2, 11, 12 (and the version labels throughout)) Several terms arrive unexplained for a reader who has only seen the previous decks: "VK residual" (page 2) is never expanded; "v1 / v3 / v3e / v4" are used as if the reader knows the decomposition version history (page 5, 11, 12, 14); "Whittle" appears only in the loop step on page 10 without ever being named on the objective slides that derive it. — fix: On page 2 write "Vold–Kalman (VK) residual" once. Add a one-line legend where versions first appear (page 5): "v1 = first amplitude targets; v3e = shipped decomposition; v4 = the model in this deck." Name the cost "Whittle cost of one cell" in the page-8 body text so page 10's "penalized Whittle fit" connects.
8. [narrative] (page 5, slide "The fix: fit amplitudes, not waveforms") The val-loss table (0.889 per-drone vs 0.878 per-drone + per-rotor) is the only number on the slide and has no reference point: the reader cannot tell whether 0.011 is meaningful, what a good value looks like, or what these losses are compared against. The slide itself then says the run is only a plumbing check, which makes the table look like evidence for nothing. — fix: Either add the comparison the narrative names (the audio-trained arm, or the codebook baseline) as a third row, or drop the table and state the plumbing result in one line: "The training path runs end to end on v1 targets; the numbers are not yet a scientific comparison." That frees space for a figure of the amplitude-envelope target.
