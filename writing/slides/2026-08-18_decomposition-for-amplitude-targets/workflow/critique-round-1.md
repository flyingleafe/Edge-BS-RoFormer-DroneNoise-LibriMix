Critic round 1 said REVISE:
1. [layout] (every content page (2-14), most visibly pages 5, 10, 13, 14) All content sits in the top ~45% of the frame; the bottom half of every slide is blank. The narrative's hard constraint #3 says "use the full slide area, avoid large white space". Wrapping cells in `align(horizon)` does nothing because the grid itself is only as tall as its content. — fix: In slides.typ give each content grid an explicit height, e.g. `#grid(rows: (1fr,), ...)` inside a `#block(height: 100%)`, or add `#v(1fr)` around the grid so content is vertically centred, and scale figures/tables up (images to `height: 100%` of the taller box) so the frame is filled.
2. [layout] (page 2, "Refined labels: did they save the generator?") The slide is ~90% empty: two short lines and one callout box floating at the top. As the first content slide it sets a weak visual tone. — fix: Either merge this recap into the results slide that follows (page 3), or fill it: put the recap sentence and the question box side by side and add the refined-vs-raw trajectory picture already used in the previous deck.
3. [clarity] (page 3, "Refined labels help, but only up to k≈25") The italic claim "Refined is the only arm with a tooth above the null past k = 25" contradicts the table as printed: the null is given as ≈ −0.8, and raw telemetry (0.23) and scaled (1.40) are both above −0.8 at k25-49. A cold reader cannot reconcile the caption with the numbers. — fix: State the actual decision rule next to the table (per-band null, or the margin threshold used), or reword the caption to what the numbers show, e.g. "refined has the largest margin over the null in every band, and is the only arm still above it at k50-80".
4. [clarity] (pages 3, 4, 11, 12 — the words "tooth", "comb readout", "null", "arm", "v1 targets", "v3e") Core vocabulary is never defined. A reader who saw only the three earlier decks does not know what a "tooth" is, what "peak-to-floor self-referenced readout" measures, or what v1/v3e are. "current (v3e); v4 refit running" on pages 11-12 is unexplained shorthand. — fix: Add one labelled inset on page 3 defining tooth = per-harmonic peak-minus-local-floor in dB, with a tiny sketch of a peak over a floor; and on pages 11-12 replace "current (v3e); v4 refit running" with a full line, e.g. "Numbers from the shipped decomposition (v3e); the v4 refit is still running."
5. [figures] (pages 7 and 9 ("The v4 model" and "Objective, part 2")) The identical figure assets/lorentzian_bumps.png appears on both slides, so page 9 gives the audience nothing new to look at and the slide reads as filler. — fix: On page 9 replace it with a figure that shows the marginalization result — for example a 1-D slice of M = S + ΣH·L over frequency, showing the smooth floor plus two Lorentzian lines summing to the modelled PSD; keep the bump figure only on page 7.
6. [figures] (page 4, right panel "Measured linewidth law") The panel title is clipped at the right edge ("γ_min" is cut off by the figure border), and both panels are small enough that the axis tick labels are borderline at 150 dpi. — fix: Regenerate assets/linewidth_law.png with a shorter title (e.g. "linewidth γ_k = max(0.6k, γ_min)") and `bbox_inches="tight"`, and increase font sizes / render the figure at the larger height the reclaimed vertical space allows.
7. [narrative] (page 12, "This objective also ranks trajectory hypotheses") The slide says refined beats telemetry on "4 of 5 windows"; narrative.md § 11 specifies "3 of 5 frozen test windows". One of the two is stale, and the punchline number of the closing result must be right. — fix: Re-read results/joint_rescore_refall summary.json, put the confirmed count on the slide, and add the units of the 0.009 margin (per cell) so the reader can judge whether the one loss matters.
8. [narrative] (page 12 (FLY124) — the missing FLY125 panel) The narrative asks for spectrogram panels on all three recordings; FLY125 appears only as a table row, and the speaker note admits "next panel omitted for time". With half the slide empty, the omission looks like a gap rather than a choice. — fix: Add the FLY125 original/comb/broadband panel beside FLY124 on the same slide (two narrow columns), or on its own slide, using the same assets recipe already used for spec_fly124.jpg.
