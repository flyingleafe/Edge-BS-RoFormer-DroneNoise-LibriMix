# Critique round 1 — REVISE

1. [layout] (slide 8/18, page-09) Accidental near-empty/orphan slide: the centered emphasis line "Physics decides where the air flows & how fast; a small learned head decides what it does to a mic." overflowed off the wind-channel diagram slide (7/18) and sits alone atop an otherwise blank page. — fix: in slides.typ around line 107 shrink assets/fig_wind_schema.png (e.g. width 78–80%) and/or fold the line-109 emphasis text into the diagram slide so the "= A wind channel" section fits on one page.
2. [clarity] (slide 1/18, page-02) The opening map says "read it back blind (§9-11), borrow the literature's version (§12-13)", but the deck has no visible section numbering and those are narrative-file section numbers, not slide numbers, so a cold reader can't resolve them. Same fragile scheme reappears as "(§6)" on slide 13/18. — fix: reference the threads by name/order instead of numbers, e.g. "read it back blind, then borrow the literature's baseline"; on slide 13 replace "(§6)" with "(the per-rotor sub-embeddings)".
3. [figures] (slide 14/18, page-15) Text-only wall (~110 words) whose central comparison is two near-identical five-digit residuals 0.99196 vs 0.99376 — indistinguishable at a glance, punchline buried. — fix: present the band residuals (0.990 / 0.874 / 0.810 vs smooth) as a small 3-row table and state the delta and its sign explicitly so "smooth wins in every band" reads instantly.
4. [layout] (slide 12/18, page-13) The VK coupling figure is stranded on a title-less continuation page because the "Reading the comb backwards" section (11/18) overflowed. — fix: split deliberately — give the figure slide a short title (e.g. "The coupling term, visually") or trim the two intro bullets on 11/18 so intro+figure+explanation fit under one title.
5. [figures] (slide 15/18, page-16) The right-hand spectrum column is essentially a flat line with one low-freq spike and adds little; half the figure wasted while the informative content is the left time-domain overlays. — fix: crop the spectrum panels to the informative low-frequency band (or drop the spectrum column) so the readable time-domain overlays fill the slide.
