# Critique round 4 — REVISE

1. [layout] (pages 28–29, slide 'Initiated work: RPS predictor achieving parity with VK optimization') Pagination broken: page 28 is empty except a repeated 'wider baselines' title, and the quadrant figure renders on page 29 with no title at all (slide heading at slides.typ:506 never appears). — fix: Trim the f1 slide (page 27) so it doesn't overflow, and shorten/fit the RPS-predictor slide so its title and figure render together on one page.
2. [layout] (page 24 (continuation of 'GP trained on the CONA data')) The corr/rel-residual table and 'matrice100 training queued next' overflow onto a title-less orphan page while page 23 is figure-only. — fix: Shrink assets/gp_dregon_overlay.png (78% -> ~55%) or place the table beside the figure so the slide fits one page.
3. [narrative] (page 26, slide 'Results: the four-way comparison') Narrative §23 and the slide's own speaker note promise a spectrogram grid real|CONA|deep|GP with per-panel msSTFT; the punchline slide is a bare 3-row table. — fix: Export the spectrogram grid from notebooks/noise_four_way_comparison.ipynb into assets/ and put the comb-err/msSTFT numbers under the panels.
4. [figures] (page 10, slide 'What the data said back: generator variants') Narrative §9 promised a spectrogram grid (rows DREGON/Michael's, cols real|OLD|v1|v2|v3, mrstft under each); only a small table is shown, so the geometry-fix win is not visible. — fix: Re-run the 07-17 report's prepare script to regenerate the grid and add at least one row of spectrograms next to the table.
5. [clarity] (pages 10 vs 26 (variant table vs four-way table)) Same metric family appears as 'free-flight MR-STFT ↑' (higher = better) on page 10 but 'msSTFT ↓' (lower = better) on page 26; a cold reader cannot tell if these are the same quantity or why the direction flips. — fix: Unify name and direction (e.g. 'msSTFT distance to real, lower = better') everywhere; if page 10's number is a different quantity, rename it and add a one-line footnote.
6. [figures] (page 17, slide 'VK results: telemetry-init refinement and blind re-annotation') Figure legend and axis labels are illegible at slide scale and the bottom log-error panel is an unreadable blur; narrative §16 asked for a refined-vs-telemetry overlay over a spectrogram. — fix: Regenerate the asset at slide-friendly size: one or two panels, larger fonts, decimate or drop the error panel.
7. [clarity] (pages 3, 7, 9 (Assumption/Hypothesis slides)) Slide 2 sets up 'Three assumptions testable on DREGON', but follow-ups are titled 'Assumption 1', then 'Hypothesis 2' and 'Hypothesis 3', breaking the promised structure. — fix: Rename consistently to Assumption 1/2/3 (or Hypothesis 1/2/3 everywhere, adjusting slide 2's wording to match).
8. [figures] (page 21, slide 'JASA-GP: original data replication') The two right-hand spectrum panels are clipped at the right edge (plot frames cut off) and have no axis labels or units. — fix: Regenerate the asset with tight_layout and labelled axes, or crop the figure to the time-domain panels only.
