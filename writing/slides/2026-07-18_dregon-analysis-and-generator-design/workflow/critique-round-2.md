Critic round 2 said REVISE (cosmetic only — round-1 fixes all verified):

1. [figures] (slide 14 / page-15, "Borrowing the literature's best: JASA-GP") The right-hand spectrum panels have their last x-axis tick clipped by the figure edge — "300" renders as a lone "3" on both rows. — fix: regenerate assets/jasa_gp_eval_slim.png in prepare.py with bbox_inches='tight' (and/or a small right margin / xlim headroom) so the 300 tick is fully visible. The crop is inside the PNG, so it must be fixed in the generating script, not by changing the typst width.
2. [tone] (slide 14 body text) "(*JASA* 159(4):3418, 2026)'s" renders with a wrong-direction curly quote because the straight apostrophe follows a ")". — fix: reword to avoid the possessive-after-paren, e.g. "A faithful replication of the GP rotor-noise model of Lee et al. (*JASA* 159(4):3418, 2026) ...".
3. [layout] (slide 14 figure caption) This is the only figure in the deck with a numbered caption ("Figure 8:") and nothing cross-references it — reads as leftover report styling. — fix: drop the auto-number on that #figure (e.g. numbering: none / supplement: none, or render the caption as a plain #text line under the image) so it matches the deck's other un-numbered figures. If other new figures in the deck also carry stray numbers, make them consistent too.
