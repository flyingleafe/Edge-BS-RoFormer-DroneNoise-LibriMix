Critic round 2 said REVISE:
1. [narrative] (page 17, slide 'Step 3: coupled solve — tracks compete for shared energy') The central step of the VK method shows only a cartoon; the coupling predicate, the block-banded Hermitian normal-equations structure, and the explaining-away mechanism exist only in the speaker note (slides.typ:263-269). This violates the narrative's non-negotiable criterion 1 (rebuild-from-slides fidelity, pseudocode explicitly welcomed): a reader cannot tell what system is solved or when two tracks couple. — fix: Add visible content next to/below the figure: the coupling condition (|f_m - f_m'| below the envelope bandwidth), a one-line statement that the coupled normal equations stay block-banded Hermitian (cheap Cholesky), and ideally a 4-line pseudocode block of the outer loop (demod -> coupled banded solve -> phase-slope update -> anneal k_max) with array shapes, in slides.typ around line 258.
2. [layout] (page 11 (continuation of 'What the data said back: generator variants')) Title-less orphan continuation page: the variant score table plus two caption lines occupy the top quarter, the remaining ~75% is blank — the same orphan pattern flagged in both previous critique rounds, and a violation of non-negotiable criterion 2 (no large unused white space). — fix: Give the page an explicit heading (e.g. '= What the data said back: the scores') and enlarge the table, or shrink the spectrogram grid on page 10 slightly and place the table beside/below it so the section is one full slide.
3. [layout] (pages 12, 14, 15, 18, 21, 27 (discussion, VK Steps 1/2/4, blind seeding v2, f1 baselines)) Systemic half-to-two-thirds empty bottom halves on the formula/text slides, directly against non-negotiable criterion 2 ('fill with the figure/formula at larger scale, or merge sparse slides'). — fix: Enlarge the formulas/tables and vertically center content (Touying config or #v spacing in slides.typ), or merge the sparsest pairs (e.g. Step 1 + Step 2 fit one slide; fold the two discussion bullets of page 12 under the page-11 table).
4. [figures] (page 23, slide 'JASA-GP: original data replication') The two right-hand spectrum panels still render with their right plot frames cut off (open/clipped spines) and no y-axis labels, so they read as accidentally cropped even though the '300' tick is now visible. — fix: Regenerate the crop in prepare.py with a slightly wider fraction so the closed right spine is included, or re-export the panels from source with tight_layout and a y-label ('spectral amplitude').
