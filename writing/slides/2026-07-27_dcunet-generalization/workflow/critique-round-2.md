1. [narrative] (page 13 "The augmentation regime, and the bug that hid it" (also pages 17–18 which depend on it)) The staging bug — the deck's central 'what broke' beat, explicitly promised on-slide by narrative §2 — exists only in the speaker note (slides.typ lines 327–337). The slide body covers only dilution, so the title's 'bug' is never shown, and later slides reference 'the protocol reset' (p17) and 'The staging bug' (p18) that a PDF-only reader has never seen explained. — fix: Add a compact on-slide box (or split into a second slide) on page 13 stating the bug in 3 lines: flatten_channels expands each chunk into 8 mono frames -> stage-2 boundary meant for epoch 10 actually landed at true epoch ~80 -> staged augs never fired in any real-data run; caught because two runs with different aug policies trained bit-identical for 40 epochs. No new figure needed.
2. [layout] (page 20 "Backup — the retracted reading") Both display formulas render garbled: `||s||^2 / ||e||^2` (slides.typ line 514) mis-parses in Typst, leaving dangling `‖²` superscripts floating above the fractions in the SI-SDR and SDR expressions. — fix: Rewrite line 514 using norm() with explicit grouping, e.g. $"SI-SDR" = alpha^2 (norm(s)^2)/(norm(e)^2) quad "SDR" = (norm(s)^2)/((1-alpha)^2 norm(s)^2 + norm(e)^2)$. This is a render fix, not a Part-1 rewrite.
3. [figures] (page 16 "What the trained head actually does") The bottom half of the slide is blank while the accumulator-degeneration figure (left) is shrunk so far that its axis labels, legend, and the 'never saturates' annotation are unreadable at 150 dpi. — fix: Use the empty space: enlarge both images (e.g. give the left panel more width or stack the two figures vertically at ~85% width each) so the lambda_t and Kalman-gain axis text is legible.
4. [tone] (page 19 "Stream sanity protocol") Slide body claims 'New gate, added this week' and presents scripts/check_stream.py as existing, but the creator log confirms it is not merged — only the speaker note discloses this. Also the bullets inside the box are center-aligned, which reads awkwardly. — fix: Change the body wording to 'gate being added this week' or 'gate design (landing this week)', and left-align the bullet list inside the box.
